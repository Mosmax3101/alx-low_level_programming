#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: the first integer
 * @b: the second integer
 *
 * Return: returns the addition result
 */

int add(int a, int b)
{
	int ans = a + b;

	return (ans);
}
